---
- name: Sincronizar Descricoes de Portas via LLDP no FortiSwitch
  hosts: all
  gather_facts: false

  vars:
    ssh_port: "{{ ansible_port | default(22) }}"

  tasks:
    - name: 1. Coletar dados do LLDP no FortiSwitch
      ansible.builtin.expect:
        command: "ssh -o StrictHostKeyChecking=no -p {{ ssh_port }} {{ ansible_user }}@{{ inventory_hostname }}"
        responses:
          "(?i)password:": "{{ ansible_password }}\n"
          ".*#": "get switch lldp neighbors-summary\nexit\n"
        timeout: 60
      delegate_to: localhost
      register: lldp_output

    - name: 2. Extrair mapeamento de Porta e Vizinho
      ansible.builtin.set_fact:
        lldp_pairs: "{{ lldp_output.stdout | regex_findall('(port\\d+)\\s+.*?\\s+([A-Za-z0-9_\\.-]+)\\s+') }}"

    - name: 3. Montar bloco CLI de alteracao de descricoes
      ansible.builtin.set_fact:
        cli_commands: >-
          config switch interface
          {% for pair in lldp_pairs %}
            {% set port = pair[0] %}
            {% set name = pair[1] | trim %}
            {% if name != '-' and name != '' %}
          edit {{ port }}
          set description to-{{ name }}
          next
            {% endif %}
          {% endfor %}
          end
          exit

    - name: 4. Executar bloco CLI de configuracao de portas
      ansible.builtin.expect:
        command: "ssh -o StrictHostKeyChecking=no -p {{ ssh_port }} {{ ansible_user }}@{{ inventory_hostname }}"
        responses:
          "(?i)password:": "{{ ansible_password }}\n"
          ".*#": "{{ cli_commands }}\n"
        timeout: 60
      delegate_to: localhost
      register: apply_desc_output

    - name: 5. Exibir resultado do processamento
      ansible.builtin.debug:
        msg: "✅ Bloco de descrições LLDP enviado e gravado com sucesso no FortiSwitch!"
